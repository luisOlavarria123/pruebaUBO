/**
 * URL base de la API. Fija por simplicidad de alcance (ver README, seccion
 * "que dejaria distinto"): en un despliegue real esto vendria de un archivo
 * de entorno reemplazado en build time.
 */
export const API_URL = 'http://localhost:8000/api';
